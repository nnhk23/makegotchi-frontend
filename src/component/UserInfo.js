import React from 'react';

const UserInfo = () => {
    // some type of condition to check if user is logged in
    // logged in => view profile
    // not logged in => sign in/sign up => <SignUp />
}
export default UserInfo